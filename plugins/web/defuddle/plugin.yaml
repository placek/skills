name: web-defuddle
version: 1.0.0
description: "Defuddle extract backend — strips a page to its main content via the `defuddle` Node CLI (kepano/defuddle, the engine behind Obsidian Web Clipper). Extract only, no API key; pair with any search backend via `web.extract_backend: defuddle`."
author: placek
license: MIT
kind: backend
provides_web_providers:
  - defuddle
